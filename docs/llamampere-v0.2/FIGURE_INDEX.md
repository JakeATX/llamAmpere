# Figure index, RELEASE_v0.2

All figures are generated by `/home/jake-k/qwen38-bench/kernel_cache_investigation/frontier/S1_speed_program_20260905/RELEASE_v0.2/make_figures.py`
(matplotlib 3.10, Agg backend, 150 dpi) into
`/home/jake-k/qwen38-bench/kernel_cache_investigation/frontier/S1_speed_program_20260905/RELEASE_v0.2/img/`.
Source paths below are relative to
`/home/jake-k/qwen38-bench/kernel_cache_investigation/frontier/S1_speed_program_20260905/`.
Re-running the script regenerates every PNG; a missing input skips that series with a printed note instead of failing.

Colors are fixed per configuration across every figure: stock TurboQuant+ blue `#2a78d6`, v0.1 with the fused MMA path off yellow `#eda100`, v0.1 as documented violet `#4a3aa7`, W7 kernels orange `#eb6834`, v0.2 aqua `#1baf7a`.

| file | what it shows | built from |
|---|---|---|
| `img/fig01_cumulative_temp1.png` | Cumulative decode rate against generated tokens for all five temperature-1.0 arms over 102,400 tokens, with the final value labelled on each line. | `W11/tps_100k/stock_per5k.csv`, `W11/tps_100k/checkpoint_per5k.csv`, `W13/tps_100k_fused/checkpoint_fused_per5k.csv`, `W13/tps_100k_fused/w7_fused_per5k.csv`, `W11/tps_100k/latest_per5k.csv` |
| `img/fig02_window5k_temp1.png` | Decode rate inside each 5,000-token window for the same five arms, showing the 65K to 90K rough stretch and each arm's floor there. | same five `*_per5k.csv` files as fig01 (`chk_tok_s` column) |
| `img/fig03_window1k_temp1.png` | The same five arms at per-1,000-token resolution, showing that the swings track local content rather than decaying with depth. | `W11/tps_100k/stock_per1k.csv`, `W11/tps_100k/checkpoint_per1k.csv`, `W13/tps_100k_fused/checkpoint_fused_per1k.csv`, `W13/tps_100k_fused/w7_fused_per1k.csv`, `W11/tps_100k/latest_per1k.csv` |
| `img/fig04_ratio_temp1.png` | Ratio of v0.2's cumulative rate to stock and to v0.1-as-documented at every 5,000-token checkpoint, with a no-change reference line at 1.00. | `W11/tps_100k/latest_per5k.csv` divided by `W11/tps_100k/stock_per5k.csv` and `W13/tps_100k_fused/checkpoint_fused_per5k.csv` |
| `img/fig05_cumulative_temp0.png` | Cumulative decode rate for the five temperature-0 greedy arms including the opt-in GPU greedy verifier, labelled as the greedy comparison and not the headline. | `W12/tps_100k_greedy/stock_per5k.csv`, `W12/tps_100k_greedy/checkpoint_per5k.csv`, `W13/tps_100k_greedy_fused/checkpoint_fused_per5k.csv`, `W12/tps_100k_greedy/latest_per5k.csv`, `W12/tps_100k_greedy_w12/latest_w12_per5k.csv` |
| `img/fig06_real_prompt_domains.png` | Decode rate and draft acceptance on real prompts across four domains for depth 3 with the static map, depth 4 with the static map, and depth 3 with the adaptive hot tail. | `W11/real_accept/results.jsonl` (aggregated by `arm` and `domain` from `predicted_n`, `predicted_ms`, `draft_n`, `draft_n_accepted`) |
| `img/fig07_depth3_vs_4.png` | MTP draft depth 3 against depth 4 in three panels: temperature 0 with the full draft head, temperature 0 with the 64K shortlist, and temperature 1.0 with the shortlist. | constants in `make_figures.py` transcribed from `W4/ANALYSIS.md` (stages 2 and 3, temperature 0); the temperature-1.0 pool is aggregated from `W11/real_accept/results.jsonl` and the RAG chain is gate G4.6 in `STATE.yaml` |
| `img/fig08_verify_pass_census.png` | Where the milliseconds of one width-4 verify pass go, with each slice tagged by the workstream that targeted it. | constants in `make_figures.py` transcribed from the per-verify-pass table in `/home/jake-k/qwen38-bench/kernel_cache_investigation/frontier/D5_atx4xs_census/ANALYSIS.md` (one level above the program directory) |
| `img/fig09_shortlist_coverage.png` | Two-panel dumbbell of draft-head coverage by domain, single-token coverage and all-three-drafted-tokens-in-window, for a raw-frequency 64K cut, the 32K shortlist and the shipped 64K shortlist. | `W3/data/report.json`, key `eval_by_domain`, sub-keys `prefix64k`, `atx_32k`, `atx_64k` |
| `img/fig10_vram_220k.png` | Peak and ready VRAM during a populated 220,000-token prompt for depth 3 without the shortlist and depth 4 with it, against the 24 GB ceiling. | `W7/raw/capacity/W7_220k_atx4xs_c225280_p220000_n16_b4096_ub1024.json`, `W9/raw/capacity/W9_220k_d4_map_atx4xs_c225280_p220000_n16_b4096_ub1024.json` |
| `img/fig11_power_clock.png` | Board power and SM clock through the temperature-1.0 runs for all five arms, with a shared legend below both panels, showing every arm pinned at the same 350 W limit in the same clock band. | `W11/tps_100k/stock_gpu.csv`, `W11/tps_100k/checkpoint_gpu.csv`, `W13/tps_100k_fused/checkpoint_fused_gpu.csv`, `W13/tps_100k_fused/w7_fused_gpu.csv`, `W11/tps_100k/latest_gpu.csv` |
| `img/fig12_accept_by_position.png` | Fraction of draft rounds accepted at each draft slot, temperature 1.0 at depth 3 and temperature 0 at depth 4, before and after the shortlist. | `W13/tps_100k_fused/summary.json` (`checkpoint_fused`), `W11/tps_100k/summary.json` (`latest`), `W12/tps_100k_greedy/summary.json` (`stock` and `latest`), `accepted_per_pos` normalized by `timings.draft_n` |
